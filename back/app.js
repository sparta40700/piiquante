const express = require("express");
const mongoose = require("mongoose");

const app = express();

/***mongoose.connect(mongodb+srv://sparta40:<Dr@gon405001>@piquante.wttud.mongodb.net/myFirstDatabase?retryWrites=true&w=majority) */
module.exports = app;
